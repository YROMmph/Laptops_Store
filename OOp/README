проект в папке OOp, единственное какие-то ошибки были с java я так понял что что-то не в той папке не могу пока решить проблему, 
но вроде всё работает, и в файле Human.Java пришлось закоментировать последние строчки выходит ошибка не понял как исправить, попробовал пару вариантом,
но ошибка та же самая в строке 228 : if (obj instanceof Human human). 
может быть вы что-то подскажите.
